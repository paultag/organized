#!/usr/bin/env python

from organized.importers.bts import BTS
import logging
import sys

logging.basicConfig(level=logging.DEBUG)

package, root_package = (sys.argv[1], sys.argv[2])

bts = BTS(package, root_package)
bts.update()
