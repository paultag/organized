#!/usr/bin/env python

from organized.importers.github import GitHub
import logging
import sys

logging.basicConfig(level=logging.DEBUG)

user, repo, root_project = (sys.argv[1], sys.argv[2], sys.argv[3])

gh = GitHub(user, repo, root_project)
gh.update()
